{
  "name": "realtime-chat-app",
  "version": "1.0.0",
  "description": "Real-Time Chat App - Vue 3 + Node.js + Socket.IO + Redis + E2E Encryption",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server"
  }
}
